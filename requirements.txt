# Core data processing
pandas>=2.3.0
numpy>=2.3.0  # pandas dependency

# Progress tracking
tqdm>=4.64.0

# Book download and format conversion
# Note: Calibre must be installed separately for MOBI to EPUB conversion
# Ubuntu/Debian: sudo apt-get install calibre
# macOS: brew install --cask calibre
# Windows: Download from https://calibre-ebook.com/download

# Jupyter for analysis (if needed)
jupyter>=1.0.0

# Deduplication pipeline dependencies
pyarrow>=15.0.0  # for parquet file handling
duckdb>=0.9.0    # for efficient SQL queries on parquet
typer[all]>=0.9.0  # for CLI interface
psutil>=5.9.0    # for memory monitoring
optuna>=3.0.0    # for hyperparameter optimization

# Shelf normalization and BERTopic pipeline dependencies
unidecode>=1.3.0  # for unicode normalization
rapidfuzz>=3.0.0  # for fuzzy string matching
symspellpy>=6.7.0  # for spelling correction (optional)
hdbscan>=0.8.0   # for clustering
scikit-learn>=1.3.0  # for ML utilities and nearest neighbors
scipy>=1.11.0    # for sparse matrices and scientific computing
networkx>=3.0    # for graph operations
sentence-transformers>=2.2.0  # for embeddings
bertopic>=0.15.0  # for topic modeling
fastapi>=0.100.0  # for API (optional)
uvicorn>=0.23.0  # for API server (optional)